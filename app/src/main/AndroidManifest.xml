<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Lets the app ask the user to stop the OS from aggressively killing it in the
         background - this is what most often causes an in-progress list to look "lost"
         on phones with strict battery managers (Xiaomi/Vivo/Oppo/Realme etc). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Needed on Android 11+ to be able to detect/launch WhatsApp Business specifically -->
    <queries>
        <package android:name="com.whatsapp.w4b" />
        <package android:name="com.whatsapp" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_action_call"
        android:label="AutoDialer"
        android:theme="@style/Theme.AutoDialer"
        android:largeHeap="true"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:label="Call Dashboard" />

        <activity
            android:name=".CallLogActivity"
            android:exported="false"
            android:label="Call Sheets" />

        <activity
            android:name=".SubscriptionActivity"
            android:exported="false"
            android:label="Subscription" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
